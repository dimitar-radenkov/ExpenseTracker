<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ExpenseTracker.Mobile"
             x:Class="ExpenseTracker.Mobile.MainPage">

    <StackLayout>
        <ListView ItemsSource="{Binding ExpensesList, Mode=TwoWay}" BackgroundColor="LightGreen"
                  HorizontalOptions="Fill" VerticalOptions="Fill">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding Description}" FontSize="Small" FontAttributes="Bold" />
                            <Label Text="{Binding Amount}" FontSize="Small"></Label>
                        </StackLayout>
                        <ViewCell.ContextActions>
                            <MenuItem Text="Delete" IsDestructive="True"></MenuItem>
                            <MenuItem Text="Send"></MenuItem>
                        </ViewCell.ContextActions>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Add" Command="{Binding AddButtonCommand}"></Button>
    </StackLayout>
</ContentPage>
